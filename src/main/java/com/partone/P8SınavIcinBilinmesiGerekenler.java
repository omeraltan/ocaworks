package main.java.com.partone;

public class P8SınavIcinBilinmesiGerekenler {
    
    /* -- Sınav için Bilinmesi Gerekenler -- */

    // "package" ve "import" ifadelerinin kullanımı,
    // "java" ve "javac" komut satırı programları için temel söz dizimi,
    // main() metodunun ne zaman süper güçlere sahip olduğu, ne zaman olmadığı,
    // import ve import static ifadeleri,
    // Classlar, metodlar, değişkenler ile ilgili erişim seviyeleri, bir class'ın ve üyenin erişim seviyesinin nasıl tanımlandığı,
    // Bir abstract class'ın abstract ve nonabstract metodlarının olabildiği, fakat sadece bir metodun abstract olması classının abstract olacağı, 
    // abstract bir class'ı imlemente eden somut bir class'ın, üst class'ının bütün abstract metodlarını implemente etmesi gerektiği ancak abstract bir class'ın, 
    // abstract classları kendi üst classından implemente etmek zorunda olmadıkları,
    // interfacelerin keşfedilmesi; bir interface, başka bir interface'i "extend" edebilir. Herhangi bir class bir interface'i "implemente" ederse bütün interface metodlarını implemente etmek zorunluluğu,
    // static, final, abstract içeren diğer modifiers'lar,
    // Bazı modifiers'ın kombine edilemediğini gördük; abstract, finali, private gibi,
    // java final diye bir nesnenin olmadığı, final olarak işaretlenmiş bir instance variable'ın hiçbir zaman değiştirilemediği, ancak başvurduğu nesnenin değiştirilebildiği,
    // Eğer "final", bir metoda uygulanırsa, o class dışında hiçbir yerde kullanılamadığı,
    // Bit metod "var-arg" parametresi ile tanımlanıyorsa, metod başına yalnızca bir değişken parametresine sahip olabildiği ve bu metodun son parametresi olması gerektiği,
    // Bir instance variable'ın türünün hiçbir zaman değiştirilemeyeceği,
    // Bir array'in aynı tipte bir çok değişken içerdiği, Arraylar'in ayrıca başka arraylar içerebildiği,
    // static değişkenler ve metodlar hakkında ne öğrendiğinizi hatırlayın, static bir metodun içinde bulunduğu class'taki bir instance variable'a doğrudan erişemediği,
    // çünkü class'ın herhangi bir örneğine açık bir referansının olmadığı,
    // Sonunda biz "enum" 'u keşfettik. Enum, sabitleri (constant) uygulamak için güvenli ve esnek bir yol olduğu, Özel bir class olduklarından, sınıflandırmaların çok basit bir şekilde bildirilebildiği veya oldukça karmaşık olabildiği,
    // Metodlar, değişkenler, constructor'lar ve özel bir class olan "inner class" 'da, özel bir class body'sinde çağrılabildiği gibi koularda bilgilendik. 

}
